export default function AlgorithmStatus({ algorithm }) {
  return (
    <div style={{ fontWeight: 600, color: "#22577a" }}>
      Algoritma: <span style={{ color: "#2d3748" }}>{algorithm}</span>
    </div>
  );
} 